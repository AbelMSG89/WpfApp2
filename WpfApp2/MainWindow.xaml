<Window x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        Title="Login - Sistema de Transportes" Height="300" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize">
    <Grid Background="#F0F0F0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="291*"/>
            <ColumnDefinition Width="109*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="60"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="60"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="#2196F3" Margin="10,10,10,0" Grid.ColumnSpan="2">
            <TextBlock Text="SISTEMA DE TRANSPORTES" 
                       HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       Foreground="White"/>
        </Border>

        <!-- Login Form -->
        <StackPanel Grid.Row="1" Margin="50,0,50,0" VerticalAlignment="Center" Grid.ColumnSpan="2">
            <TextBlock Text="Usuario:" FontWeight="Bold" Margin="0,0,0,2"/>
            <TextBox x:Name="txtUsuario" Height="18" FontSize="12" Margin="0,0,0,1"/>

            <TextBlock Text="Contraseña:" FontWeight="Bold" Margin="0,0,0,2"/>
            <PasswordBox x:Name="txtPassword" Height="18" FontSize="12" Margin="0,0,0,5"/>

            <Button x:Name="btnLogin" Content="INGRESAR" 
                    Height="20" FontSize="12" FontWeight="Bold"
                    Background="#4CAF50" Foreground="White"
                    Click="btnLogin_Click"/>
        </StackPanel>

        <!-- Footer -->
        <TextBlock Grid.Row="2" Text="Versión 1.0" 
                   HorizontalAlignment="Left" 
                   VerticalAlignment="Center" 
                   FontSize="10" 
                   Foreground="Gray" Margin="176,0,0,0"/>
    </Grid>
</Window>
